<%= form_for(prog) do |f| %>
  <% if prog.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(prog.errors.count, "error") %> prohibited this prog from being saved:</h2>

      <ul>
      <% prog.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :Название %>
    <%= f.text_field :Название %>
  </div>

  <div class="field">
    <%= f.label :Лицензия %>
    <%= f.check_box :Лицензия %>
  </div>

  <div class="field">
    <%= f.label :Тип_лицензии %>
    <%= f.text_field :Тип_лицензии %>
  </div>

  <div class="field">
    <%= f.label :Лиценз_№ %>
    <%= f.text_field :Лиценз_№ %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
